# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in matchd.gemspec
gemspec

gem "simplecov", "~> 0.20.0", require: false, group: :test
